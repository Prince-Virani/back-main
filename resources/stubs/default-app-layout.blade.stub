<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
 <meta name="csrf-token" content="{{ csrf_token() }}">
    <title>@yield('title',$Websitedetails->website_name)</title>
    <link rel="icon" type="image/webp" href="https://admin.videolite.in/storage/website-logo/{{ $Websitedetails?->id }}/{{ $Websitedetails?->favicon_path ?? 'favicon.webp' }}">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css">

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <!-- FontAwesome for Icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">

    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
    <link rel="stylesheet" href="{{ asset('themes/' . $Websitedetails->website_theme . '/css/style.css') }}">
   
    @if(isset($TagManagers) && $TagManagers->isNotEmpty())
    @foreach($TagManagers as $tag)
        {!! $tag->content !!}
    @endforeach
@endif

  @if(isset($ads['interstitial']) && $ads['interstitial']->isNotEmpty())
{!! $ads['interstitial']->first()->ad_code !!}
@endif
</head>
<body>
   
    <!-- Top Bar for Ads -->
@if(isset($ads['header']) && $ads['header']->isNotEmpty())
    <div class="top-bar top-bar-align-right">
        <div class="inside-top-bar grid-container">
            <aside id="block-21" class="widget inner-padding widget_block">
             {!! $ads['header']->first()->ad_code !!} 
            </aside>
        </div>
    </div>
@endif


    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg navbar-light bg-light shadow-sm">
        <div class="container">
            <a class="navbar-brand" href="{{ url('/') }}">
                <img src="https://admin.videolite.in/storage/website-logo/{{ $Websitedetails?->id }}/{{ $Websitedetails?->logo_path ?? 'logo.webp' }}" alt="Logo">
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item"><a class="nav-link" href="{{ url('/') }}">Home</a></li>
                    <li class="nav-item"><a class="nav-link" href="{{ url('/contact') }}">Contact Us</a></li>
                    @foreach($commonpages as $commonpage)
                        <li class="nav-item">
                            <a class="nav-link" href="{{ route('commonpage.show', $commonpage->slug) }}">{{ $commonpage->page_name }}</a>
                        </li>
                    @endforeach
                </ul>
            </div>
        </div>
    </nav>

    <!-- Main Content -->
    <div class="container mt-4">
        @yield('content')
    </div>

     <!-- Footer -->
<footer class="bg-dark text-white py-4 mt-5">
    <div class="container">
        <div class="row align-items-center text-center text-md-start">
            
            <!-- Left: Logo & Address -->
            <div class="col-md-4 mb-3 mb-md-0">
                <a  href="{{ url('/') }}">
                <img src="https://admin.videolite.in/storage/website-logo/{{ $Websitedetails?->id }}/{{ $Websitedetails?->logo_path ?? 'logo.webp' }}" alt="Logo" class="mb-2" style="max-height: 50px;"></a>
               <p class="mb-1">{{ $Websitedetails->website_name }}</p>
                <p class="mb-0">{{ $Websitedetails->company_address }}</p>
                <p>Email: {{ $Websitedetails->email }}</p>
            </div>

            <!-- Middle: Popular Categories -->
            <div class="col-md-4 mb-3 mb-md-0">
                <h5 class="fw-bold text-white">POPULAR CATEGORY</h5>
                <ul class="list-unstyled">
                    @if(isset($categoryCounts) && count($categoryCounts) > 0)
                        @foreach($categoryCounts as $category => $count)
                            <li class="category-item">
                                <a href="{{ url('/category/' . str_replace(' ', '-', $category)) }}" class="category-link">
                                    {{ $category }}
                                </a>
                                <span class="category-count">{{ $count }}</span>
                            </li>
                        @endforeach
                    @else
                        <li class="text-white">No categories available</li>
                    @endif
                </ul>
            </div>

            <!-- Right: Subscribe & Social Media -->
            <div class="col-md-4 text-center text-md-end">
                @if(session('success'))
                <div class="alert alert-success">
                    {{ session('success') }}
                </div>
                @endif
                <h5>📩 Subscribe for Updates</h5>
                <form action="{{ route('subscribe') }}" method="POST" class="d-flex flex-column flex-sm-row justify-content-center">
                @csrf 
                    <input type="email" name="email" class="form-control me-sm-2 mb-2 mb-sm-0" placeholder="Enter your email" required>
                    <button type="submit" class="btn btn-primary">Subscribe</button>
                </form>
                @if ($errors->any())
                    <div class="alert alert-danger">
                    <ul>
                        @foreach ($errors->all() as $error)
                            <li>{{ $error }}</li>
                        @endforeach
                    </ul>
                    </div>
                @endif

                <h5 class="mt-3">Follow Us</h5>
                <div class="d-flex justify-content-center justify-content-md-end mt-3">
                    <a href="#" class="text-white me-3"><i class="fab fa-facebook-f fa-lg"></i></a>
                    <a href="#" class="text-white me-3"><i class="fab fa-twitter fa-lg"></i></a>
                    <a href="#" class="text-white me-3"><i class="fab fa-instagram fa-lg"></i></a>
                    <a href="#" class="text-white me-3"><i class="fab fa-linkedin-in fa-lg"></i></a>
                    <a href="#" class="text-white"><i class="fab fa-youtube fa-lg"></i></a>
                </div>
            </div>
        </div>

        <!-- Bottom Section: Copyright Left & Quick Links Right -->
        <div class="d-flex flex-column flex-md-row justify-content-between align-items-center mt-5">
            <!-- Left: Copyright -->
            <p class="mb-0 order-2 order-md-1 pt-3 pt-md-0">&copy; {{ date('Y') }} · All rights reserved. <a href="{{ url('/') }}" class="text-white text-decoration-none">{{ $Websitedetails->website_name }}</a></p>

            <!-- Right: Quick Links -->
            <ul class="list-unstyled quick-links d-flex mb-0 order-1 order-md-2">
                 @foreach($commonpages as $commonpage)
                <li class="me-3">
                    <a class="w-1/2 min-w-[140px]" href="{{ route('commonpage.show', $commonpage->slug) }}">{{ $commonpage->page_name }}</a>
                </li>
                 @endforeach
                <li class="me-3 "><a href="{{ route('contact') }}" class="text-white text-decoration-none">Contact Us</a></li>
                
            </ul>
        </div>
    </div>
</footer>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

</body>
<!-- Footer Ad Section -->
@if(isset($ads['footer']) && $ads['footer']->isNotEmpty())
    <div class="footer-ad">
        <div class="inside-footer-ad grid-container">
            <aside id="footer-block-21" class="widget inner-padding widget_block">
                <div class="ad-code">
                    {!! $ads['footer']->first()->ad_code !!} <!-- Render the first footer ad code -->
                </div>
            </aside>
        </div>
    </div>
@endif
</html>
